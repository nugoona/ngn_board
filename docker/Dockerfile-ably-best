# 1. Python 3.9 슬림 이미지 기반
FROM python:3.9-slim

# 2. 환경 변수 설정
ENV PYTHONUNBUFFERED=1

# 3. 필수 유틸리티 및 Google Chrome 설치 (방식 변경: 직접 다운로드)
# apt-key 사용을 피하고 .deb 파일을 직접 설치하여 의존성을 자동 해결합니다.
RUN apt-get update && apt-get install -y \
    wget \
    gnupg \
    unzip \
    curl \
    --no-install-recommends \
    && wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb \
    && apt-get install -y ./google-chrome-stable_current_amd64.deb \
    && rm google-chrome-stable_current_amd64.deb \
    && rm -rf /var/lib/apt/lists/*

# 4. 작업 디렉토리 설정
WORKDIR /app

# 5. 라이브러리 설치
# (프로젝트 루트에 있는 requirements.txt를 가져옵니다)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 6. 소스 코드 복사
# (tools 폴더에 있는 스크립트를 컨테이너의 /app으로 복사합니다)
COPY tools/ably_crwal.py .

# 7. 실행 명령어
CMD ["python", "ably_crwal.py"]
